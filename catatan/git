git init
git remote add origin [url]
git add --all
git config user.email "email_github"
git config user.name "username_github"
git commit -m "init"
git config --local credential.helper "" (tidak wajib)
git push -u origin master
